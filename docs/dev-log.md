# HotFixAmbulance — development log

Append-only timeline of TDD cycles. The latest entry is at the bottom. Entries are written automatically by [.claude/hooks/post-tool-use.ps1](../.claude/hooks/post-tool-use.ps1) and serve as exam evidence that the work is AI-driven.

| When (UTC) | Phase | Cycle step | Note |
| --- | --- | --- | --- |
| 2026-06-16 | 0.1 | scaffold | Created repo skeleton, `.gitignore`, `.editorconfig`, README, placeholder folders. |
| 2026-06-16 10:23 | 1.1–1.5 | green | dotnet — 9-project sln + Core domain (Severity, LogEntry, ErrorGroup) with 16 passing tests |
| 2026-06-16 10:31 | 2.1�2.3 | green | dotnet � Elastic module: LogQuery, IElasticLogSource, ElasticLogIngestor (TDD, 8 tests), ElasticsearchLogSource v8 + Polly + search_after, SerilogDocumentMapper (4 tests). 30/30 unit tests passing. |
| 2026-06-16 10:35 | 3.1�3.3 | green | dotnet � Analysis module: MessageNormalizer, IAnalysisStrategy, AnalysisRule, DefaultRules (NullRef/Timeout/Deadlock/Validation/5xx), HeuristicAnalyzer, LlmAnalysisStrategy stub. TDD with 18 new tests. 48/48 unit tests passing. |
| 2026-06-16 10:41 | 4.1�4.3 | green | dotnet � GitInsights module: ApisConfig, IGitRepoCache + LibGit2SharpRepoCache (LocalAppData cache), IGitHistoryReader + LibGit2SharpHistoryReader, FixHintBuilder. TDD with 11 new tests. 59/59 unit tests passing. |
| 2026-06-16 10:49 | 5.1�5.3 | green | dotnet � Persistence (EF Core SQLite + TriageRun + TriageRunRepository), Api (TriageService orchestrator + Minimal API endpoints). TDD with 10 new unit tests + 2 integration tests via WebApplicationFactory. 69 unit + 2 integration tests passing. |
| 2026-06-16 10:56 | 6.1-6.2 | green | dotnet � CLI wiring (CliArgs parser + CliRenderer + in-process Host running TriageService). 15 new unit tests (14 CliArgs + 1 CliRenderer). Slash command .claude/commands/hot-fix-ambulance.md already invokes the CLI. 84 unit + 2 integration tests passing. |
| 2026-06-16 11:04 | 7.1-7.4 | green | dotnet+npm � Backend: GetByIdAsync + GET /api/triage/runs/{id} rehydrating TriageResult + JsonStringEnumConverter (Severity now serializes as 'Error'). Frontend: scaffolded Vite + React 19 + TS + TanStack Table v8 + TanStack Query v5 + Tailwind v3 + Vitest + RTL + ESLint. 12-column TriageTable with severity sort and AI columns. 88 unit/integration backend tests + 8 frontend tests passing. All 4 pre-commit gates green. |
| 2026-06-16 11:08 | 8.1-8.2 | green | dotnet+pwsh � demo-api: .NET 10 Minimal API with Serilog (Console + File + optional Elasticsearch sink via ECS formatter), 3 instrumented endpoints (POST /orders -> NRE, GET /payments/{id} -> upstream timeout, GET /users/{id} -> ArgumentOutOfRangeException for negative ids). Smoke-tested manually: 500/500/504 returned with structured Serilog events containing Application=demo-api + RequestPath + StatusCode + CorrelationId. scripts/demo.ps1 orchestrator builds demo-api, starts it, hammers endpoints, runs the CLI. Backend tests still 88/88 green. |
| 2026-06-16 11:11 | 9.1 | green | docs � wrote docs/exam.md (Softuni rubric): 1-page idea + 9 modules with approach/workflow/tests/AI-tool/prompts, challenges + tool comparison, working-system evidence, screenshot capture plan, and repo link. |

## Customization log

Out-of-cycle changes (refactors, bug fixes, dev-workflow improvements) tracked outside the strict TDD timeline above.

| When (UTC) | Area | Result | Note |
| --- | --- | --- | --- |
| 2026-06-16 13:29 | refactor | green | Renamed ErrorGroup.Purpose -> Suggestion across Core/Analysis/Cli/Api + frontend (types.ts, TriageTable). UI column header is now "Suggestion for Error". |
| 2026-06-16 13:29 | analysis | green | AnalysisRule now carries both Suggestion (WHAT) and HowToFix (HOW). DefaultRules expanded so every rule (NullRef/Timeout/Deadlock/Validation/5xx) emits distinct, related text in the two AI columns. HeuristicAnalyzer.EnrichWithRule populates both. |
| 2026-06-16 13:29 | tests | green | Updated HeuristicAnalyzerTests (5 renamed + new Analyze_fills_distinct_howtofix_for_each_matched_rule), CliRendererTests (Purpose -> Suggestion), TriageTable.test.tsx (suggestion field + new "Suggestion for Error" column-header assertion + distinct-text assertion). Backend 89/89, frontend 9/9 green. |
| 2026-06-16 13:29 | docs | green | .claude/agents/log-analyzer.md JSON schema and Markdown table use suggestion / "Suggestion for Error"; .claude/commands/hot-fix-ambulance.md wording aligned. |
| 2026-06-16 13:29 | bugfix | green | HotFixAmbulance.Api: appsettings.json ships Apis:ConfigPath = "" which slipped past Program.cs's ?? fallback (empty string is not null) -> ApisConfig.LoadFromFile("") threw and every POST /api/triage returned 500. Replaced with IsNullOrWhiteSpace check + new ApisConfigPathResolver that probes bin dir, repo config/apis.config.json, then config/apis.config.example.json. `dotnet run` from any CWD now resolves the config. |
| 2026-06-16 13:29 | demo | green | Verified end-to-end: POST /api/triage/demo-api returns 22 groups / 48 logs with populated suggestion + howToFix; FixHintBuilder overrides heuristic HowToFix with real origin/main commits where keywords match. |
| 2026-06-16 13:59 | demo-api | green | Added DemoApi.BrokenServices (CustomerRepository, OrderProcessor, PaymentGateway) so endpoints throw real NREs / TimeoutExceptions with full stack traces. Program.cs now resolves the top user frame via System.Diagnostics.StackTrace and pushes ExceptionType/StackFile/StackSymbol/StackLine through Serilog LogContext, so the ECS sink ships them as both top-level fields and `error.stack_trace`. |
| 2026-06-16 13:59 | core+elastic | green | LogEntry/ErrorGroup gained nullable StackFile/StackSymbol/StackLine. SerilogDocumentMapper prefers the pre-extracted fields and falls back to a compiled regex parser over `error.stack_trace` that skips System./Microsoft./Serilog. frames and accepts Windows drive-letter paths. Two new mapper tests cover both code paths. |
| 2026-06-16 13:59 | analysis | green | New internal SuggestionBuilder generates a dynamic Suggestion from the exception leaf + message + endpoint + stack symbol (e.g. "NullReferenceException — dereferenced `Customer.Email` after a lookup returned null in `OrderProcessor.GetCustomerEmail` on POST /orders → 500."). HeuristicAnalyzer always uses the builder; matched rules now contribute HowToFix only. Existing assertions on canonical keywords (timeout/deadlock/validation/5xx) still hold. |
| 2026-06-16 13:59 | gitinsights | green | GitSearchQuery gained PreferredFile; LibGit2SharpHistoryReader does a two-pass search (file-basename hits first, then keyword hits, de-duped by SHA). FixHintBuilder forwards group.StackFile, caps results to MaxResults defensively, and emits a multi-line evidence block: `path:line · Type.Method` header + `└ likely introduced in {sha} ({date}) by {Author}` + `└ "{subject}"` + related-commit lines. ExtractKeywords also splits StackSymbol. |
| 2026-06-16 13:59 | frontend | green | ErrorGroup type + TriageTable now render a 2nd-line stack frame badge (`file:line · symbol`, `data-testid="stackframe"`, monospace) under the exception type cell when present. New RTL test verifies it. 10/10 vitest passing. |
| 2026-06-16 13:59 | gates | green | All 4 pre-commit gates clean: `dotnet build` (0 warnings, no CA1859/CA1861), 91 unit + 2 integration tests, `dotnet format --verify-no-changes` (after applying whitespace fixes for CRLF in regenerated FixHintBuilder.cs), `eslint --max-warnings 0`. |
