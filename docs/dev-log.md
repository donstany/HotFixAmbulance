# HotFixAmbulance — development log

Append-only timeline of TDD cycles. The latest entry is at the bottom. Entries are written automatically by [.claude/hooks/post-tool-use.ps1](../.claude/hooks/post-tool-use.ps1) and serve as exam evidence that the work is AI-driven.

| When (UTC) | Phase | Cycle step | Note |
| --- | --- | --- | --- |
| 2026-06-16 | 0.1 | scaffold | Created repo skeleton, `.gitignore`, `.editorconfig`, README, placeholder folders. |
| 2026-06-16 10:23 | 1.1–1.5 | green | dotnet — 9-project sln + Core domain (Severity, LogEntry, ErrorGroup) with 16 passing tests |
| 2026-06-16 10:31 | 2.1�2.3 | green | dotnet � Elastic module: LogQuery, IElasticLogSource, ElasticLogIngestor (TDD, 8 tests), ElasticsearchLogSource v8 + Polly + search_after, SerilogDocumentMapper (4 tests). 30/30 unit tests passing. |
| 2026-06-16 10:35 | 3.1�3.3 | green | dotnet � Analysis module: MessageNormalizer, IAnalysisStrategy, AnalysisRule, DefaultRules (NullRef/Timeout/Deadlock/Validation/5xx), HeuristicAnalyzer, LlmAnalysisStrategy stub. TDD with 18 new tests. 48/48 unit tests passing. |
| 2026-06-16 10:41 | 4.1�4.3 | green | dotnet � GitInsights module: ApisConfig, IGitRepoCache + LibGit2SharpRepoCache (LocalAppData cache), IGitHistoryReader + LibGit2SharpHistoryReader, FixHintBuilder. TDD with 11 new tests. 59/59 unit tests passing. |
| 2026-06-16 10:49 | 5.1�5.3 | green | dotnet � Persistence (EF Core SQLite + TriageRun + TriageRunRepository), Api (TriageService orchestrator + Minimal API endpoints). TDD with 10 new unit tests + 2 integration tests via WebApplicationFactory. 69 unit + 2 integration tests passing. |
| 2026-06-16 10:56 | 6.1-6.2 | green | dotnet � CLI wiring (CliArgs parser + CliRenderer + in-process Host running TriageService). 15 new unit tests (14 CliArgs + 1 CliRenderer). Slash command .claude/commands/hot-fix-ambulance.md already invokes the CLI. 84 unit + 2 integration tests passing. |
| 2026-06-16 11:04 | 7.1-7.4 | green | dotnet+npm � Backend: GetByIdAsync + GET /api/triage/runs/{id} rehydrating TriageResult + JsonStringEnumConverter (Severity now serializes as 'Error'). Frontend: scaffolded Vite + React 19 + TS + TanStack Table v8 + TanStack Query v5 + Tailwind v3 + Vitest + RTL + ESLint. 12-column TriageTable with severity sort and AI columns. 88 unit/integration backend tests + 8 frontend tests passing. All 4 pre-commit gates green. |
| 2026-06-16 11:08 | 8.1-8.2 | green | dotnet+pwsh � demo-api: .NET 10 Minimal API with Serilog (Console + File + optional Elasticsearch sink via ECS formatter), 3 instrumented endpoints (POST /orders -> NRE, GET /payments/{id} -> upstream timeout, GET /users/{id} -> ArgumentOutOfRangeException for negative ids). Smoke-tested manually: 500/500/504 returned with structured Serilog events containing Application=demo-api + RequestPath + StatusCode + CorrelationId. scripts/demo.ps1 orchestrator builds demo-api, starts it, hammers endpoints, runs the CLI. Backend tests still 88/88 green. |
| 2026-06-16 11:11 | 9.1 | green | docs � wrote docs/exam.md (Softuni rubric): 1-page idea + 9 modules with approach/workflow/tests/AI-tool/prompts, challenges + tool comparison, working-system evidence, screenshot capture plan, and repo link. |

## Customization log

Out-of-cycle changes (refactors, bug fixes, dev-workflow improvements) tracked outside the strict TDD timeline above.

| When (UTC) | Area | Result | Note |
| --- | --- | --- | --- |
| 2026-06-16 13:29 | refactor | green | Renamed ErrorGroup.Purpose -> Suggestion across Core/Analysis/Cli/Api + frontend (types.ts, TriageTable). UI column header is now "Suggestion for Error". |
| 2026-06-16 13:29 | analysis | green | AnalysisRule now carries both Suggestion (WHAT) and HowToFix (HOW). DefaultRules expanded so every rule (NullRef/Timeout/Deadlock/Validation/5xx) emits distinct, related text in the two AI columns. HeuristicAnalyzer.EnrichWithRule populates both. |
| 2026-06-16 13:29 | tests | green | Updated HeuristicAnalyzerTests (5 renamed + new Analyze_fills_distinct_howtofix_for_each_matched_rule), CliRendererTests (Purpose -> Suggestion), TriageTable.test.tsx (suggestion field + new "Suggestion for Error" column-header assertion + distinct-text assertion). Backend 89/89, frontend 9/9 green. |
| 2026-06-16 13:29 | docs | green | .claude/agents/log-analyzer.md JSON schema and Markdown table use suggestion / "Suggestion for Error"; .claude/commands/hot-fix-ambulance.md wording aligned. |
| 2026-06-16 13:29 | bugfix | green | HotFixAmbulance.Api: appsettings.json ships Apis:ConfigPath = "" which slipped past Program.cs's ?? fallback (empty string is not null) -> ApisConfig.LoadFromFile("") threw and every POST /api/triage returned 500. Replaced with IsNullOrWhiteSpace check + new ApisConfigPathResolver that probes bin dir, repo config/apis.config.json, then config/apis.config.example.json. `dotnet run` from any CWD now resolves the config. |
| 2026-06-16 13:29 | demo | green | Verified end-to-end: POST /api/triage/demo-api returns 22 groups / 48 logs with populated suggestion + howToFix; FixHintBuilder overrides heuristic HowToFix with real origin/main commits where keywords match. |
| 2026-06-16 13:59 | demo-api | green | Added DemoApi.BrokenServices (CustomerRepository, OrderProcessor, PaymentGateway) so endpoints throw real NREs / TimeoutExceptions with full stack traces. Program.cs now resolves the top user frame via System.Diagnostics.StackTrace and pushes ExceptionType/StackFile/StackSymbol/StackLine through Serilog LogContext, so the ECS sink ships them as both top-level fields and `error.stack_trace`. |
| 2026-06-16 13:59 | core+elastic | green | LogEntry/ErrorGroup gained nullable StackFile/StackSymbol/StackLine. SerilogDocumentMapper prefers the pre-extracted fields and falls back to a compiled regex parser over `error.stack_trace` that skips System./Microsoft./Serilog. frames and accepts Windows drive-letter paths. Two new mapper tests cover both code paths. |
| 2026-06-16 13:59 | analysis | green | New internal SuggestionBuilder generates a dynamic Suggestion from the exception leaf + message + endpoint + stack symbol (e.g. "NullReferenceException — dereferenced `Customer.Email` after a lookup returned null in `OrderProcessor.GetCustomerEmail` on POST /orders → 500."). HeuristicAnalyzer always uses the builder; matched rules now contribute HowToFix only. Existing assertions on canonical keywords (timeout/deadlock/validation/5xx) still hold. |
| 2026-06-16 13:59 | gitinsights | green | GitSearchQuery gained PreferredFile; LibGit2SharpHistoryReader does a two-pass search (file-basename hits first, then keyword hits, de-duped by SHA). FixHintBuilder forwards group.StackFile, caps results to MaxResults defensively, and emits a multi-line evidence block: `path:line · Type.Method` header + `└ likely introduced in {sha} ({date}) by {Author}` + `└ "{subject}"` + related-commit lines. ExtractKeywords also splits StackSymbol. |
| 2026-06-16 13:59 | frontend | green | ErrorGroup type + TriageTable now render a 2nd-line stack frame badge (`file:line · symbol`, `data-testid="stackframe"`, monospace) under the exception type cell when present. New RTL test verifies it. 10/10 vitest passing. |
| 2026-06-16 13:59 | gates | green | All 4 pre-commit gates clean: `dotnet build` (0 warnings, no CA1859/CA1861), 91 unit + 2 integration tests, `dotnet format --verify-no-changes` (after applying whitespace fixes for CRLF in regenerated FixHintBuilder.cs), `eslint --max-warnings 0`. |
| 2026-06-16 14:24 | docs | green | Teacher review materials: docs/teacher-workflow.md (8-step Do/Observe/Proves walkthrough + 'convinced of' table + troubleshooting cheatsheet) and docs/teacher-workflow-cli.md (copy-paste CLI cheat sheet, 8 numbered command blocks + quick-recovery section for stale hfa-es / busy 5333 / locked 5283). |
| 2026-06-18 10:00 | 10.1 demo-api | green | demo-api refactored to REAL Entity Framework CRUD: removed mock services (CustomerRepository/OrderProcessor/PaymentGateway/PricingEngine), added 7 entity models (CustomerRecord/OrderRecord/PaymentRecord/UserRecord/InvoiceRecord/TransferRecord/PricingRecord) with EF Core mappings (unique indexes, FKs, decimal precision). DatabaseFailureSimulator now performs real async CRUD via SaveChangesAsync. All 8 endpoints use real DbContext. Build OK (1.8s). |
| 2026-06-18 10:00 | 10.1 verify | green | End-to-end smoke against real MSSQL (hfa-mssql, localhost:14333) -> POST /invoices/duplicate yields genuine SqlClient.SqlException 2601 (IX_Invoices_InvoiceNumber duplicate key) wrapped as Microsoft.EntityFrameworkCore.DbUpdateException; full stack trace flows into Serilog -> Elasticsearch. No simulated exceptions left. |
| 2026-06-18 10:00 | 10.1 gates | green | All 4 pre-commit gates: 97 backend tests (unit+integration) pass, dotnet format clean, 12 frontend tests pass, eslint --max-warnings 0 clean. Build succeeded in 14.0s. |
| 2026-06-18 10:00 | 10.1 docs | green | demo-api/README.md rewritten with real CRUD docs: entity models, per-endpoint CRUD operations, MSSQL/SQLite provider config, "No Mocking" guarantee section. |
| 2026-06-18 10:00 | 10.1 commit | green | git commit 40759e9 "Phase 10.1: Refactor demo-api for real Entity Framework CRUD operations" -- 8 files changed, 558 insertions(+), 182 deletions(-). |
| 2026-06-18 10:00 | 10.2 e2e | green | Full stack restart: killed leftover dotnet/vite processes (PIDs 68936/4536/13228/36184/38232/52080/62180/62940/69664/72516/73364), ran scripts/demo.ps1 -WithElastic -KeepRunning -> MSSQL+ES infra up, demo-api produced 24 error requests, es-bootstrap reindexed logs-2026.06.18 -> hfa-mapped via ecs_to_fields pipeline (492 docs, 593 error/warn hits via alias logs-mapped-2026.06.18), CLI wrote analysis id b186d604-5f9e-4835-a8fe-5740c15a0837 (335 logs) to hotfix.db. |
| 2026-06-18 10:00 | 10.2 api+ui | green | Started HotFixAmbulance.Api on :5283 with HFA_Persistence__ConnectionString pinned to repo-root hotfix.db (had to kill stale PID 42232 holding port; default SQLite path made API see empty db). Started Vite dev server on :5173. Smoke OK: GET http://localhost:5173/api/triage/runs/{id} -> totalLogs=335 via vite proxy. UI opened at http://localhost:5173/?analysisId=b186d604-5f9e-4835-a8fe-5740c15a0837&api=demo-api. |
| 2026-06-18 10:18 | 11.1 line-context | green | Added FileLineContext record (ResolvedPath/StartLine/OffendingLine/Lines/Blame) in HotFixAmbulance.GitInsights; extended IGitHistoryReader with GetLineContextAsync. LibGit2SharpHistoryReader.GetLineContextAsync reads blob from origin/main tree (via ResolvePathInTree basename/suffix walker), slices a +/- contextLines window around the offending line, and runs repo.Blame(path, BlameStrategy.Default) to attribute the hunk covering that exact line to (sha,date,author,subject). |
| 2026-06-18 10:18 | 11.1 fix-hint UI | green | FixHintBuilder.BuildAsync now calls GetLineContextAsync first when StackFile+StackLine present, then SearchCommitsAsync; emits hint of the form 'Where to fix: path:line - symbol' followed by 'code (from origin/main):' block with >> marker on offending line, then lame: <sha> (date) by <author> "<subject>", then elated: commits (deduped via HashSet<string> emittedShas). Snippet-only output is allowed when SearchCommitsAsync finds nothing. |
| 2026-06-18 10:18 | 11.1 stack regex | green | Fixed SerilogDocumentMapper.FrameRegex to tolerate .NET's modern async stack frame prefix sync <ReturnType>  (e.g. t async Task<decimal> DemoApi.X.YAsync(...) in .../file.cs:line 311). Old regex required symbol right after t , so all real EF DbUpdateException frames yielded null StackFile/StackLine and FixHintBuilder produced commit-only hints. Added failing test TryMap_parses_modern_async_frame_with_return_type_modifier first, then loosened regex to ^\\s*at\\s+.*?(?<sym>[\\w]+(?:\\.[\\w<>`]+)+)\\s*\\(.... |
| 2026-06-18 10:18 | 11.1 tests | green | Added 3 FixHintBuilder tests (embeds_code_snippet_and_blame_when_line_context_available, invokes_line_context_with_stack_file_and_line, returns_snippet_only_when_search_returns_no_commits) using static readonly string[] fields (SnippetLines/BlameFiles/SnippetOnlyLines) to satisfy CA1861. Plus 1 mapper regression test. Total: 98 unit + 3 integration backend + 12 frontend = all pass. |
| 2026-06-18 10:18 | 11.1 demo.ps1 | green | scripts/demo.ps1 now prefers config/apis.config.json (which maps demo-api -> file:///C:/.../HotFixAmbulance branch main) over .example.json so FixHintBuilder can mine origin/main of the real repo. First demo run triggers LibGit2SharpRepoCache.EnsureUpToDateAsync to clone the local repo into %LOCALAPPDATA%/HotFixAmbulance/repos/demo-api/. |
| 2026-06-18 10:18 | 11.1 e2e | green | End-to-end smoke (analysis e349a47c-1c8f-4275-a5e1-3e14d728336d, 117 groups, 18 with parsed stackFile): /invoices/duplicate group now has howToFix='Where to fix: demo-api/DemoDatabase.cs:277 - DatabaseFailureSimulator.TriggerDuplicateInvoiceAsync\\n  code (from origin/main):\\n     275 | CreatedAtUtc = DateTime.UtcNow,\\n     276 | });\\n  >> 277 | await _db.SaveChangesAsync(ct).ConfigureAwait(false);\\n     278 |\\n     279 | _db.Invoices.Add(new InvoiceRecord\\n  blame: acd1c28 by Stanislav-Stanev ...' rendered as <pre whitespace-pre-wrap font-mono> in the How to fix column. |
| 2026-06-18 10:18 | 11.1 gates | green | All 4 pre-commit gates: 98 backend unit + 3 integration tests pass, dotnet format clean (auto-fixed CRLF -> LF on new GitInsights files), 12 frontend tests pass, eslint --max-warnings 0 clean. |
| 2026-06-18 10:18 | 11.1 line-context | green | Added FileLineContext record (ResolvedPath/StartLine/OffendingLine/Lines/Blame) in HotFixAmbulance.GitInsights; extended IGitHistoryReader with GetLineContextAsync. LibGit2SharpHistoryReader.GetLineContextAsync reads blob from origin/main tree (via ResolvePathInTree basename/suffix walker), slices a +/- contextLines window around the offending line, and runs repo.Blame(path, BlameStrategy.Default) to attribute the hunk covering that exact line to (sha,date,author,subject). |
| 2026-06-18 10:18 | 11.1 fix-hint UI | green | FixHintBuilder.BuildAsync now calls GetLineContextAsync first when StackFile+StackLine present, then SearchCommitsAsync; emits hint of the form 'Where to fix: path:line - symbol' followed by 'code (from origin/main):' block with >> marker on offending line, then lame: <sha> (date) by <author> "<subject>", then elated: commits (deduped via HashSet<string> emittedShas). Snippet-only output is allowed when SearchCommitsAsync finds nothing. |
| 2026-06-18 10:18 | 11.1 stack regex | green | Fixed SerilogDocumentMapper.FrameRegex to tolerate .NET's modern async stack frame prefix sync <ReturnType>  (e.g. t async Task<decimal> DemoApi.X.YAsync(...) in .../file.cs:line 311). Old regex required symbol right after t , so all real EF DbUpdateException frames yielded null StackFile/StackLine and FixHintBuilder produced commit-only hints. Added failing test TryMap_parses_modern_async_frame_with_return_type_modifier first, then loosened regex to ^\\s*at\\s+.*?(?<sym>[\\w]+(?:\\.[\\w<>`]+)+)\\s*\\(.... |
| 2026-06-18 10:18 | 11.1 tests | green | Added 3 FixHintBuilder tests (embeds_code_snippet_and_blame_when_line_context_available, invokes_line_context_with_stack_file_and_line, returns_snippet_only_when_search_returns_no_commits) using static readonly string[] fields (SnippetLines/BlameFiles/SnippetOnlyLines) to satisfy CA1861. Plus 1 mapper regression test. Total: 98 unit + 3 integration backend + 12 frontend = all pass. |
| 2026-06-18 10:18 | 11.1 demo.ps1 | green | scripts/demo.ps1 now prefers config/apis.config.json (which maps demo-api -> file:///C:/.../HotFixAmbulance branch main) over .example.json so FixHintBuilder can mine origin/main of the real repo. First demo run triggers LibGit2SharpRepoCache.EnsureUpToDateAsync to clone the local repo into %LOCALAPPDATA%/HotFixAmbulance/repos/demo-api/. |
| 2026-06-18 10:18 | 11.1 e2e | green | End-to-end smoke (analysis e349a47c-1c8f-4275-a5e1-3e14d728336d, 117 groups, 18 with parsed stackFile): /invoices/duplicate group now has howToFix='Where to fix: demo-api/DemoDatabase.cs:277 - DatabaseFailureSimulator.TriggerDuplicateInvoiceAsync\\n  code (from origin/main):\\n     275 | CreatedAtUtc = DateTime.UtcNow,\\n     276 | });\\n  >> 277 | await _db.SaveChangesAsync(ct).ConfigureAwait(false);\\n     278 |\\n     279 | _db.Invoices.Add(new InvoiceRecord\\n  blame: acd1c28 by Stanislav-Stanev ...' rendered as <pre whitespace-pre-wrap font-mono> in the How to fix column. |
| 2026-06-18 10:18 | 11.1 gates | green | All 4 pre-commit gates: 98 backend unit + 3 integration tests pass, dotnet format clean (auto-fixed CRLF -> LF on new GitInsights files), 12 frontend tests pass, eslint --max-warnings 0 clean. |
| 2026-06-18 11:45 | 11.2 UI/UX | green | frontend - new ExpandableCell + CellDetailModal show 3-line preview (line-clamp-3) with three-dot expand button that opens a styled modal (Copy + close); applied to Message, Suggestion, How-to-fix columns. Removed HTTP column. Added centered amber log time-range pill (Logs from ... -> ... (duration)) in the analysis header between title and analysis id. 13 frontend tests pass (added expand-modal test); eslint clean. |
| 2026-06-18 12:15 | 12.A | green | Phase 12.A backend - new TimeWindow value object in HotFixAmbulance.Core (Relative/Absolute factories, From<To validation, 30-day default max span). Added TimeWindow overloads to ElasticLogIngestor.FetchAsync + TriageService.RunAsync that take an absolute UTC window; old TimeSpan overloads kept as thin wrappers (back-compat for Phase B+). 9 new TimeWindowTests; total 107 unit + 3 integration tests green. |
| 2026-06-18 12:24 | 12.B | green | Phase 12.B backend API - POST /api/triage/{apiName} now accepts ?fromUtc=&toUtc= (absolute UTC range) as a mutex alternative to ?lookbackHours=. Returns 400 with ProblemDetails when both are supplied, only one of from/to, or range is inverted. TriageResult now exposes FromUtc, ToUtc, IsTruncated. ElasticLogIngestor.FetchAsync(TimeWindow) returns new IngestionResult(Logs, IsTruncated) where IsTruncated=true when ElasticOptions.MaxDocuments cap hit. New GET /api/apis returns sorted known api names. Rehydrate synthesizes window from RequestedAtUtc-Lookback (real persistence lands in Phase C). Tests: 110 unit (+3 ingestor), 9 integration (+6 endpoint), 13 frontend - all green. |
| 2026-06-18 12:39 | 12.C | green | Phase 12.C backend persistence - TriageRun gains nullable FromUtc/ToUtc (DateTimeOffset?) persisted as ISO-8601 TEXT via EF value converters. TriageService.RunAsync(TimeWindow) now writes the absolute window onto the TriageRun row. Rehydrate falls back to RequestedAtUtc-Lookback when null (back-compat for pre-12.C rows). Program.cs adds EnsureWindowColumnsAsync shim: PRAGMA table_info(TriageRuns) + ALTER TABLE ADD COLUMN FromUtc/ToUtc TEXT NULL when missing (gated by IsRelational so InMemory tests are no-op). New tests: TriageRunRepositoryTests.AddAsync_persists_FromUtc_and_ToUtc_when_provided + AddAsync_allows_null_FromUtc_and_ToUtc_for_back_compat; TriageEndpointsTests.POST_absolute_then_GET_run_round_trips_FromUtc_and_ToUtc. Totals: 112 unit + 10 integration + 13 frontend - all green. |
| 2026-06-18 12:43 | 12.D | green | Phase 12.D CLI - CliArgs now accepts `--from <iso> --to <iso>` (mutex with --lookback) as absolute UTC window. Validation: both flags must be supplied together; cannot combine with --lookback; --from must be earlier than --to; ISO-8601 timestamps. CliArgs exposes nullable FromUtc/ToUtc and a ToWindow(now) helper that returns TimeWindow.Absolute when both set, else TimeWindow.Relative(now, Lookback). When absolute, Lookback is exposed as (To-From) for back-compat. Cli/Program.cs calls TriageService.RunAsync(apiName, window). 9 new CliArgsTests (absolute parsing, mutex, completeness, inverted range, ISO validation, ToWindow modes). Totals: 121 unit + 10 integration + 13 frontend, all green. |
| 2026-06-18 12:48 | 12.E | green | Phase 12.E frontend - new TimeRangePicker component with 7 presets (15m/1h/6h/24h/7d/30d/Custom). Custom mode reveals 2 datetime-local inputs in browser local time, validated (both required, From<To, span<=30 days) and converted to UTC ISO via new Date(localValue).toISOString(). New Run-analysis bar: API dropdown sourced from GET /api/apis (lists all known API names sorted), Range picker, Run button. On success, navigates URL to ?analysisId=<new>&api=<name> and reroutes the analysis view. Added 'Rerun this window' button in the analysis header that pre-fills the picker with the persisted From/To and immediately re-runs. types.ts adds fromUtc/toUtc/isTruncated to TriageResult + new TimeRangeSelection discriminated union. api.ts adds fetchApiNames + runTriage(apiName, range). Header now displays the requested window pill (formatWindow(r.fromUtc, r.toUtc)) instead of group min/max. Added 'results truncated' amber badge when r.isTruncated. Tests: 6 new TimeRangePicker tests (preset emit, default 24h pressed, custom reveal, absolute UTC emit, From<To validation, prefill); App.test.tsx mock updated with fromUtc/toUtc/isTruncated/fetchApiNames/runTriage. 19 frontend tests + eslint clean. |
| 2026-06-18 12:52 | 12.F | green | Phase 12.F config defaults - new TriageOptions (Triage:DefaultLookbackHours=24, Triage:MaxRangeDays=30) bound from appsettings.json with ValidateDataAnnotations. POST /api/triage/{apiName} no longer hardcodes 24h; uses opts.DefaultLookbackHours when no lookback/from-to params. MaxRangeDays is passed as maxSpan to TimeWindow.Relative/Absolute, returning 400 ProblemDetails when exceeded. 2 new TriageOptionsTests (defaults, SectionName) + 1 integration test (POST_triage_with_absolute_range_exceeding_MaxRangeDays_returns_400). Totals: 123 unit + 11 integration + 19 frontend, all green. Closes Phase 12 refactor. |
